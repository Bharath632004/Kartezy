server:
  port: 8112

spring:
  application:
    name: security-dashboard

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.kartezy.securitydashboard: INFO
