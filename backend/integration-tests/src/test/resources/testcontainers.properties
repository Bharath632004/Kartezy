# Testcontainers configuration
# Use Docker API version negotiation for compatibility
docker.client.strategy=org.testcontainers.dockerclient.EnvironmentAndSystemPropertyClientProviderStrategy

# Pin Docker API version for compatibility with local Docker daemon
# Required: local Docker daemon requires minimum API version 1.40
docker.api.version=1.44
