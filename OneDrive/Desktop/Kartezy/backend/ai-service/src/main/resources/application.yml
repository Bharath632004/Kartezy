server:
  port: 8082

spring:
  application:
    name: ai-service

# Eureka client configuration (assuming service discovery via Eureka)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/